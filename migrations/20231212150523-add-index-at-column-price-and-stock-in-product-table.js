'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up (queryInterface, Sequelize) {
    /**
     * Add altering commands here.
     *
     * Example:
     * await queryInterface.createTable('users', { id: Sequelize.INTEGER });
     */
    await queryInterface.sequelize.query(`CREATE INDEX CONCURRENTLY IF NOT EXISTS "idx_price" ON "Products" USING btree ("price")`);
    await queryInterface.sequelize.query(`CREATE INDEX CONCURRENTLY IF NOT EXISTS "idx_stock" ON "Products" USING btree ("stock")`);
  },

  async down (queryInterface, Sequelize) {
    /**
     * Add reverting commands here.
     *
     * Example:
     * await queryInterface.dropTable('users');
     */
  }
};
